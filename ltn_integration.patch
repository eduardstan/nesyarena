diff --git a/experiments/configs/E5_mnist.yaml b/experiments/configs/E5_mnist.yaml
index ed57978..d517e9d 100644
--- a/experiments/configs/E5_mnist.yaml
+++ b/experiments/configs/E5_mnist.yaml
@@ -14,11 +14,11 @@ hidden: 128
 treatment:
   grid: 3
   held_queries: [[0],[2],[5],[0,1,2],[3,4,5]]   # subsets of the 6 path-proofs
-  suts: [exact, addmult, top1, top3, minmax]
+  suts: [exact, addmult, top1, top3, minmax, ltn_product, ltn_godel]
 control:
   digits: [0, 1, 2]
   train_terms: [[0,2],[1,1],[2,0]]
   held_terms: [[0,0],[1,1],[2,2]]
   # top3 has k >= P here (3 mutually exclusive proofs): predicted identical
   # to exact — included so the identity is measured, not asserted
-  suts: [exact, addmult, top1, top3, minmax]
+  suts: [exact, addmult, top1, top3, minmax, ltn_product, ltn_godel]
diff --git a/experiments/configs/E6_facttable.yaml b/experiments/configs/E6_facttable.yaml
index f471dd7..c30b579 100644
--- a/experiments/configs/E6_facttable.yaml
+++ b/experiments/configs/E6_facttable.yaml
@@ -15,4 +15,4 @@ optimizer: { steps: 4000, lr: 0.05 }
 # transfer differs from min-clamp addmult beyond seed noise (direction
 # open: no blackout, but unclamped push with no value feedback);
 # P2 — on the disjoint control (no saturation) addmult_st == addmult == exact.
-suts: [exact, addmult, addmult_st, top1, top3, minmax]
+suts: [exact, addmult, addmult_st, top1, top3, minmax, ltn_product, ltn_godel]
diff --git a/experiments/configs/E6_pixels.yaml b/experiments/configs/E6_pixels.yaml
index 287ec3c..e220630 100644
--- a/experiments/configs/E6_pixels.yaml
+++ b/experiments/configs/E6_pixels.yaml
@@ -16,11 +16,11 @@ treatment:
   structure: { P: 4, L: 3, c: 2 }
   held_queries: [[0],[1],[2],[3],[0,1,2]]
   # addmult_st per finding F-3; registered predictions as in E6_facttable
-  suts: [exact, addmult, addmult_st, top1, top3, minmax]
+  suts: [exact, addmult, addmult_st, top1, top3, minmax, ltn_product, ltn_godel]
 control:
   pA_true: [0.5, 0.3, 0.2]
   pB_true: [0.3, 0.4, 0.3]
   train_terms: [[0,2],[1,1],[2,0]]   # query: zA + zB == 2 (mutually exclusive)
   held_terms: [[0,0],[1,1],[2,2]]    # transfer query: zA == zB
   # top3: k >= P on this structure — predicted identical to exact (measured)
-  suts: [exact, addmult, top1, top3, minmax]
+  suts: [exact, addmult, top1, top3, minmax, ltn_product, ltn_godel]
diff --git a/experiments/e5_mnist.py b/experiments/e5_mnist.py
index 244715a..23e064f 100644
--- a/experiments/e5_mnist.py
+++ b/experiments/e5_mnist.py
@@ -38,7 +38,8 @@ from nesyarena.learning import BatchStructure, prov_value  # noqa: E402
 HERE = os.path.dirname(os.path.abspath(__file__))
 OUT = os.environ.get("NESYARENA_OUT", os.path.join(HERE, "..", "out"))
 DATA = os.path.join(HERE, "..", ".data")
-SUT_K = {"exact": None, "addmult": None, "top1": 1, "top3": 3, "minmax": None}
+SUT_K = {"exact": None, "addmult": None, "top1": 1, "top3": 3, "minmax": None,
+         "ltn_product": None, "ltn_godel": None}
 EPS = 1e-4
 
 
@@ -155,9 +156,15 @@ def cat_value(name, terms, PA, PB, k=None):
             k = len(terms)
         order = torch.argsort(vals.detach(), dim=1, descending=True)[:, :k]  # frozen
         return vals.gather(1, order).sum(dim=1)
-    if name == "minmax":
+    if name in ("minmax", "ltn_godel"):
         mins = torch.stack([torch.minimum(PA[:, i], PB[:, j]) for (i, j) in terms], dim=1)
         return mins.amax(dim=1)
+    if name == "ltn_product":
+        acc = vals[:, 0]
+        for j in range(1, vals.shape[1]):
+            b = vals[:, j]
+            acc = acc + b - acc * b
+        return acc
     raise ValueError(name)
 
 
diff --git a/experiments/e5b_noise_ablation.py b/experiments/e5b_noise_ablation.py
index faa935c..4edba09 100644
--- a/experiments/e5b_noise_ablation.py
+++ b/experiments/e5b_noise_ablation.py
@@ -48,7 +48,7 @@ OUT = os.environ.get("NESYARENA_OUT", os.path.join(HERE, "..", "out"))
 DIGITS = [0, 1, 2]
 TERMS_TR = [(0, 2), (1, 1), (2, 0)]
 TERMS_HE = [(0, 0), (1, 1), (2, 2)]
-SUTS = ["exact", "top1", "minmax"]
+SUTS = ["exact", "top1", "minmax", "ltn_product", "ltn_godel"]
 ETAS = [0.0, 0.15, 0.3, 0.5]
 
 
@@ -103,10 +103,17 @@ def run(seeds=(0, 1, 2), epochs=6, n_train=3000, n_test=1500, batch=64, lr=1e-3)
                     PA = torch.softmax(net(XA_t), 1).clamp(EPS, 1.0).numpy()
                     PB = torch.softmax(net(XB_t), 1).clamp(EPS, 1.0).numpy()
                     vals = np.stack([PA[:, i] * PB[:, j] for (i, j) in TERMS_HE], 1)
-                    vh = (vals.max(1) if name == "top1"
-                          else np.stack([np.minimum(PA[:, i], PB[:, j])
-                                         for (i, j) in TERMS_HE], 1).max(1)
-                          if name == "minmax" else vals.sum(1))
+                    if name == "top1":
+                        vh = vals.max(1)
+                    elif name in ("minmax", "ltn_godel"):
+                        vh = np.stack([np.minimum(PA[:, i], PB[:, j])
+                                       for (i, j) in TERMS_HE], 1).max(1)
+                    elif name == "ltn_product":
+                        vh = vals[:, 0]
+                        for j in range(1, vals.shape[1]):
+                            vh = vh + vals[:, j] - vh * vals[:, j]
+                    else:
+                        vh = vals.sum(1)
                     res[f"{name}@{eta}"]["trans"].append(
                         float(np.mean(np.abs(vh - truth_he))))
                     res[f"{name}@{eta}"]["cal"].append(
diff --git a/experiments/e6_facttable.py b/experiments/e6_facttable.py
index 611670c..26949d2 100644
--- a/experiments/e6_facttable.py
+++ b/experiments/e6_facttable.py
@@ -43,7 +43,8 @@ from nesyarena.oracle import wmc  # noqa: E402
 HERE = os.path.dirname(os.path.abspath(__file__))
 OUT = os.environ.get("NESYARENA_OUT", os.path.join(HERE, "..", "out"))
 SUT_K = {"exact": None, "addmult": None, "addmult_st": None,
-         "top1": 1, "top3": 3, "minmax": None}
+         "top1": 1, "top3": 3, "minmax": None,
+         "ltn_product": None, "ltn_godel": None}
 EPS = 1e-4
 
 
@@ -104,16 +105,28 @@ def run_treatment(cfg):
 
 def cat_value(name, terms, pA, pB, k):
     """Disjoint categorical sum. exact and addmult share one path: on
-    mutually exclusive proofs add-mult IS distribution semantics (H9)."""
+    mutually exclusive proofs add-mult IS distribution semantics (H9).
+    ltn_product does NOT join that identity: its Or-prod (a+b-ab) assumes
+    INDEPENDENCE, not mutual exclusivity, so on this disjoint control it
+    systematically UNDER-estimates (opposite bias from the G1 overlap
+    treatment, where it over-estimates by assuming independence where
+    facts are actually shared/correlated). Genuinely different failure
+    mode, not a bug -- report it, don't "fix" it.
+    ltn_godel is byte-identical to minmax by definition (And=min, Or=max)."""
     vals = torch.stack([pA[i] * pB[j] for (i, j) in terms])
     if name in ("exact", "addmult", "addmult_st"):  # disjoint: no clamp active
         return vals.sum()
     if name.startswith("top"):
         order = torch.argsort(vals.detach(), descending=True)[:k]  # frozen
         return vals[order].sum()
-    if name == "minmax":
+    if name in ("minmax", "ltn_godel"):
         mins = torch.stack([torch.minimum(pA[i], pB[j]) for (i, j) in terms])
         return mins.amax()
+    if name == "ltn_product":
+        acc = vals[0]
+        for v in vals[1:]:
+            acc = acc + v - acc * v
+        return acc
     raise ValueError(name)
 
 
diff --git a/experiments/e6_pixels.py b/experiments/e6_pixels.py
index a549e05..86b7874 100644
--- a/experiments/e6_pixels.py
+++ b/experiments/e6_pixels.py
@@ -41,7 +41,8 @@ from nesyarena.learning import BatchStructure, prov_value  # noqa: E402
 HERE = os.path.dirname(os.path.abspath(__file__))
 OUT = os.environ.get("NESYARENA_OUT", os.path.join(HERE, "..", "out"))
 SUT_K = {"exact": None, "addmult": None, "addmult_st": None,
-         "top1": 1, "top3": 3, "minmax": None}
+         "top1": 1, "top3": 3, "minmax": None,
+         "ltn_product": None, "ltn_godel": None}
 EPS = 1e-4
 
 
@@ -145,9 +146,15 @@ def cat_value(name, terms, PA, PB):
         k = min(int(name[3:]), len(terms))
         order = torch.argsort(vals.detach(), dim=1, descending=True)[:, :k]  # frozen
         return vals.gather(1, order).sum(dim=1)
-    if name == "minmax":
+    if name in ("minmax", "ltn_godel"):
         mins = torch.stack([torch.minimum(PA[:, i], PB[:, j]) for (i, j) in terms], dim=1)
         return mins.amax(dim=1)
+    if name == "ltn_product":
+        acc = vals[:, 0]
+        for j in range(1, vals.shape[1]):
+            b = vals[:, j]
+            acc = acc + b - acc * b
+        return acc
     raise ValueError(name)
 
 
diff --git a/experiments/scorecard.py b/experiments/scorecard.py
index 3be3bc7..554e70b 100644
--- a/experiments/scorecard.py
+++ b/experiments/scorecard.py
@@ -37,7 +37,8 @@ OUT = os.environ.get("NESYARENA_OUT", os.path.join(HERE, "..", "out"))
 
 # E6 result keys per SUT name used in the learning experiments
 E6_KEY = {"exact-wmc": "exact", "add-mult(clamped)": "addmult",
-          "top-1-proofs": "top1", "top-3-proofs": "top3", "min-max-prob": "minmax"}
+          "top-1-proofs": "top1", "top-3-proofs": "top3", "min-max-prob": "minmax",
+          "ltn:product": "ltn_product", "ltn:godel": "ltn_godel"}
 
 
 def liveness_battery() -> dict[str, float]:
diff --git a/src/nesyarena/learning/structures.py b/src/nesyarena/learning/structures.py
index 543cba9..f164b0f 100644
--- a/src/nesyarena/learning/structures.py
+++ b/src/nesyarena/learning/structures.py
@@ -93,6 +93,19 @@ class BatchStructure:
         per_proof = torch.stack([Pb[:, pi].amin(dim=1) for pi in self.pidx], dim=1)
         return per_proof.amax(dim=1)
 
+    def ltn_prod(self, Pb: torch.Tensor) -> torch.Tensor:
+        """LTN, connettivi prodotto (fo.AndProd/fo.OrProbSum): per-proof AND
+        e' gia' il prodotto (== scores()); tra i proof si fa OR-prod in
+        sequenza: a `or` b = a + b - a*b. minmax() sopra e' gia', byte per
+        byte, la variante Godel (And=min, Or=max) -- nessun metodo separato
+        serve per quella."""
+        sc = self.scores(Pb)                      # (B, P) AND-prod per proof
+        acc = sc[:, 0]
+        for j in range(1, sc.shape[1]):
+            b = sc[:, j]
+            acc = acc + b - acc * b
+        return acc
+
 
 def prov_value(name: str, S: BatchStructure, Pb: torch.Tensor,
                k: int | None = None) -> torch.Tensor:
@@ -104,6 +117,8 @@ def prov_value(name: str, S: BatchStructure, Pb: torch.Tensor,
         return S.addmult_st(Pb)
     if name.startswith("top"):
         return S.topk(Pb, k if k is not None else int(name[3:]))
-    if name == "minmax":
+    if name == "minmax" or name == "ltn_godel":
         return S.minmax(Pb)
+    if name == "ltn_product":
+        return S.ltn_prod(Pb)
     raise ValueError(name)
diff --git a/src/nesyarena/ltn_provenance.py b/src/nesyarena/ltn_provenance.py
new file mode 100644
index 0000000..33b78aa
--- /dev/null
+++ b/src/nesyarena/ltn_provenance.py
@@ -0,0 +1,96 @@
+"""Provenance basata su LTNtorch (product fuzzy logic / Real Logic).
+
+Segue l'interfaccia di suts.Provenance (vedi docs/ADAPTERS.md):
+- value(): il valore che LTN calcola per la query, aggregando i proof
+  (ognuno = congiunzione AND-prod/AND-min dei fatti che lo compongono) con
+  OR-probsum/OR-max sui proof alternativi.
+- grad(): il gradiente autentico di LTN, ottenuto con autograd reale
+  (non finite differences), come richiesto da ADAPTERS.md ("if your
+  backend is differentiable, grad should return its own gradients").
+- claimed_semantics: "distribution semantics" per la variante prodotto
+  (coincide ESATTAMENTE con l'oracolo WMC quando i proof sono disgiunti;
+  diverge quando condividono fatti, perche' And/Or-prod assumono
+  indipendenza -- e' un finding da riportare, non un bug da correggere);
+  "boolean reachability" per la variante Godel (che coincide byte-per-byte
+  col semiring BOOLEAN di algebra.py / con la Provenance MinMax).
+
+Nota di scope: questa Provenance lavora su proof-DNF gia' enumerati
+(program.proof_supports), come AddMult/TopK/MinMax. Per esperimenti di
+profondita' ricorsiva (E2, E7) servirebbe un Adapter vero che valuti LTN
+sulla struttura a regole del GroundProgram (stile engine.py/T_P), perche'
+l'enumerazione dei proof esplode con la ricorsione -- fuori scope qui.
+"""
+
+from __future__ import annotations
+
+import torch
+import ltn.fuzzy_ops as fo
+
+from .suts import Provenance
+
+_AND_PROD = fo.AndProd()
+_OR_PROD = fo.OrProbSum()
+_AND_GODEL = fo.AndMin()
+_OR_GODEL = fo.OrMax()
+
+
+class _LTNProvenanceBase(Provenance):
+    """Scheletro comune: aggrega i proof con AND-dentro-il-proof e
+    OR-tra-i-proof, usando SEMPRE le classi di ltn.fuzzy_ops (mai
+    reimplementate a mano) e SEMPRE autograd reale per il gradiente."""
+
+    and_op = None  # istanza di una classe *ConnectiveOperator di fuzzy_ops
+    or_op = None
+
+    def _proof_value(self, pr, prob_tensors: dict) -> torch.Tensor:
+        v = torch.tensor(1.0)
+        for f in pr:
+            v = self.and_op(v, prob_tensors[f])
+        return v
+
+    def _formula_value(self, proofs, prob_tensors: dict) -> torch.Tensor:
+        vals = [self._proof_value(pr, prob_tensors) for pr in proofs]
+        acc = vals[0]
+        for v in vals[1:]:
+            acc = self.or_op(acc, v)
+        return acc
+
+    def value(self, proofs, probs) -> float:
+        if not proofs:
+            return 0.0
+        prob_tensors = {f: torch.tensor(float(p)) for f, p in probs.items()}
+        return self._formula_value(proofs, prob_tensors).item()
+
+    def grad(self, proofs, probs) -> dict:
+        if not proofs:
+            return {}
+        facts = sorted(set().union(*proofs), key=repr)
+        prob_tensors = {
+            f: torch.tensor(float(probs[f]), requires_grad=True) for f in facts
+        }
+        out = self._formula_value(proofs, prob_tensors)
+        out.backward()
+        # AndMin/OrMax non sono ovunque differenziabili (ties): PyTorch
+        # restituisce comunque un sottogradiente valido -- lo riportiamo
+        # cosi' com'e', e' il comportamento autentico del backend.
+        return {f: prob_tensors[f].grad.item() for f in facts}
+
+
+class LTNProdProvenance(_LTNProvenanceBase):
+    """LTN con connettivi prodotto (fo.AndProd, fo.OrProbSum):
+    And = a*b, Or = a+b-a*b."""
+
+    name = "ltn:product"
+    claimed = "distribution semantics"
+    and_op = _AND_PROD
+    or_op = _OR_PROD
+
+
+class LTNGodelProvenance(_LTNProvenanceBase):
+    """LTN con connettivi Godel (fo.AndMin, fo.OrMax): And = min, Or = max.
+    Corrisponde ESATTAMENTE al semiring BOOLEAN dell'arena (algebra.py)."""
+
+    name = "ltn:godel"
+    claimed = "boolean reachability"
+    and_op = _AND_GODEL
+    or_op = _OR_GODEL
diff --git a/src/nesyarena/suts.py b/src/nesyarena/suts.py
index 74f8c2c..d41411c 100644
--- a/src/nesyarena/suts.py
+++ b/src/nesyarena/suts.py
@@ -177,4 +177,6 @@ class LSE(Provenance):
 
 def registry() -> list[Provenance]:
     """The standard SUT lineup for sweeps and scorecards."""
-    return [ExactWMC(), AddMult(clamp=True), TopK(1), TopK(3), MinMax()]
+    from .ltn_provenance import LTNProdProvenance, LTNGodelProvenance
+    return [ExactWMC(), AddMult(clamp=True), TopK(1), TopK(3), MinMax(),
+            LTNProdProvenance(), LTNGodelProvenance()]
